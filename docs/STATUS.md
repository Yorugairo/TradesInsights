# Status

> The living one-pager. Update at the end of every working session and every completed task ID. Sessions are ephemeral — this file plus git history is the durable memory.

**Local-run handoff:** `docs/handoff-local.md` — the turnkey guide for continuing in a local Claude Code environment to unblock the datacenter-egress sources (Pierce PALS `contractorInfo` is the priority: it's the only public supply of Pierce contractor phone/UBI/license, and the phone-matching lane is already built and waiting on it). Includes real capture targets (permit IDs 1067557/1067558/1067513…), the endpoint template, fixture paths, and a paste-ready first-session kickoff prompt.

**Current task:** **Valuation truth, orphan regression guard, plan close-out (2026-07-28).** Plan `.claude/PRPs/plans/completed/valuation-truth-orphan-guard-and-plan-closeout.plan.md`; report `.claude/PRPs/reports/valuation-truth-orphan-guard-and-plan-closeout-report.md`; baseline regenerated at `docs/data-quality-audit-2026-07-28.md`. **Two of the three requested workstreams were DISPROVEN by measurement and are recorded, not quietly dropped.** **(1) There is no valuation parser defect anywhere in the fleet, and the audit I shipped this morning is what said otherwise.** Its per-source table counted `valuationUsd IS NULL` without asking whether anything was published to extract, and I used that ranking within the hour to recommend a week of parser work. Measured against `raw_fields_json`: bellevue carries a `VALUATION` key on every row but non-empty on **one** (value `0`); puyallup publishes only `FeeAmount`, a permit **fee**; olympia publishes no valuation field at all; pierce captures **1,596 of the 1,655** `buildingValuation` values that exist (96%, and the 59-row residue is **entirely `"0"`**); and **every one** of king's 1,451 nulls is a literal `jobValue "0"` on a mechanical/fire/sprinkler permit. Both adapters already implement the governing rule — *"0/negative valuation is not stated, never $0"* (`arcgis-permits.ts:143`, `king-permit-reports.ts:277`) — and they are right; mapping `FeeAmount` would be fabrication. The metric is now **stated / not published / DROPPED**, sorted by DROPPED, with the caveat printed beside it. **Hosted: `DROPPED = 0` across all 26 public sources.** The key allow-list deliberately excludes fee-shaped keys — widening it to `%amount%`/`%cost%` would resurrect the phantom backlog, and a test asserts `FeeAmount` lands in `not published`. **(2) Bellevue, measured properly with count-only probes (nothing ingested):** 440,444 rows all-time, 35,468 with `VALUATION > 0` (8.1% — the config's figure is exact), 5,901 inside the adapter's trailing 120-day window, and **0 of those carry a valuation**. By ISSUEDDATE: 6,503 since 2020, 3,171 since 2023, 996 since 2025, **0 since 2026-01-01**; the last issued permit carrying one is **2025-11-21**. Bellevue STOPPED PUBLISHING the field ~8 months ago, so widening the window would recover **history, not current leads**. Recorded in `config/sources.yaml`; no ingestion change. **(3) The 31 orphan projects + 15 mis-merged parcel matches have already self-healed — that STATUS item is now CLOSED.** All 46 PALS records sit on the same project as their ArcGIS twin; graph-wide there are **0 permit ids resolving to more than one project, 0 projects with no active resolution, 0 conflicting registered ids**. Resolver pass 1b + `reevaluatePendingReviews` did it silently. So instead of a remediation script — which would have been a repair tool for a healed defect, i.e. a tool that damages healthy data next time — `splitExternalIds` is now a HARD INVARIANT, **grouped on `(source_id, external_id)`** because a permit number is unique only within a jurisdiction, and it names the offending ids because a count alone is unfixable. **(4) Plan close-out found more open work than expected: 5 archived, 5 annotated, not 9 archived.** The three-check rule (report found by CONTENT not filename prefix; STATUS; the plan's own acceptance criteria) is what caught it. Archived: cockpit-seam-performance-and-degradation, cockpit-ui-first-class-product (all 4 phases reported), registry-insights-golive-taxonomy-warmnet-sources (only governance-deferred), source-fleet-flow-verification (only owner-secrets blocked), webs-omwbe-solicitation-ingestion (phase 3 deferred ON EVIDENCE). Annotated in place with what remains: queue-cockpit (**A1–A6 + E1–E2**), trades-read-model-verticalization (**tasks 3–4**; task 2 abandoned by measurement), gym-market-comparator-windowed-peers (**task 5 NOT STARTED**), puget-sound-source-expansion (**Auburn/Clark/Burien**), registry-insights-dataflow-solis-inference (**WS-D, WS-E.2/3/4, WS-C.2/3**). 1,300/1,300 tests (+5), 33/33 e2e, typecheck + lint clean, **`pnpm eval:run` byte-identical** (precision 1, recall 0.9609375). Zero adapter changes, zero migrations, zero scoring inputs touched. **BLOCKING EVERYTHING (owner):** the scheduled fleet fires and dies in ~10s — `DATABASE_URL secret is not set`. Every source's last run is manual. Ten secrets needed; the workflow also continues past its own guard and hits `pnpm: command not found`.

**Last completed:** **Data hygiene round 2 — org name quality, review-queue honesty, codified audit (2026-07-28).** Plan `.claude/PRPs/plans/completed/data-hygiene-round-2.plan.md`; baseline `docs/data-quality-audit-2026-07-28.md`. **(1) "Is this string a company" is now DATA, and it had five definitions.** The plan said the junk/entity regexes lived only in `cockpit_opportunities_v1`; they are byte-identical in migrations 0025 (twice), 0026, 0027 and 0028, plus a TypeScript transcription in `packages/intelligence/src/org-activity.ts` — and the CURRENT owner of the cockpit view is **0027, not 0026**, so rebuilding from the plan's pointer would have silently dropped the three corroboration columns. `classifyOrgNameQuality` (regexes lifted verbatim) is now the only definition; migration 0039 adds `organizations.name_quality` + CHECK + a partial junk index; the resolver stamps it at write time. **The backfill checks itself against the SQL it replaces** — the same query that reads the names asks Postgres to evaluate the original predicates, and the run reports every disagreement. **Live: 6,220 scanned, driftSamples 0** (2,659 business / 3,503 person_or_unknown / 58 junk). **(2) 0040 was written so apply-order cannot bite**, and production proved it: the view predicate falls back to the old regexes when `name_quality IS NULL`, and with all 6,220 rows unstamped the view was byte-identical (4,251 rows / 1,100 with `gc_name` / 551 distinct GCs) — then identical again after the backfill. Zero rows moved. The strict swap (`= 'business'`) is deliberately deferred: 3,503 rows are `person_or_unknown` (a sole proprietor, NOT junk), so the two predicates are not equivalent. **(3) The review queue is mostly comparable, not evidence-starved — the plan's premise was wrong.** `reclassifyComparanda` re-asks the `same_address_name_mismatch` question against the candidate project's ORGANIZATION names instead of its canonical name. Of 776 pending rows (784 at planning time), **596 (77%) DO carry org names and stay actionable**, only **164** have nothing to compare, and **16 upgraded** on names the old comparison structurally could not see (`"City of Ruston Grid Resilience Project"` ↔ `CITY OF RUSTON`). `pnpm review name-audit` cross-checks the population exactly and still reports 100% `none`, confirming round 1's category-error finding rather than contradicting it. **Nothing is decided:** status/decided_by/decided_at/decision_note are never written, only two `features_json` keys. **(4) `pnpm audit:data` replaces the hand-run SQL session,** splitting INVARIANTS (exit 1: underived corroboration, duplicate events on the 0035 key, >10 unresolved-and-unqueued) from METRICS (reported, never fatal — a 56% person-name rate is a fact). **Hosted run: INVARIANTS OK**, and it reproduces the plan's numbers exactly (16,231 projects / 15,031 located / 41,305 events / 0 dupes / 2,490 pending split 1,622+815+53 / 13,537 untagged of which 3,126 untaggable / BUILDING 3,573, BF 853, RIGHT-OF-WAY 678 / 1,090 priority-band opportunities with no stated valuation). **(5) The T5 sweep found what it was built to find: the junk tier is NOT clean.** 16 junk-classified orgs carry identifiers, 2 of them registry-bound — `123 ELECTRIC SERVICE INC`, `365 PLUMBING`, `AUBURN SCHOOL DISTRICT NO 408`, `BETHEL SCH DIST #403`… — all false positives of the inherited `[0-9]{3,}` rule. Not a regression (the same regex has excluded them since 0025), but now named instead of buried in a view body. **Owner decision, not automated; nothing deleted.** **(6) "1,200 unlocated projects" was never a backlog** — 552 have no address to geocode, 646 were geocoded and came back empty, and **2** are genuinely waiting. The audit's first version conflated them into "never attempted: 554"; the four buckets now partition the project set and a test asserts they sum. 1,295/1,295 tests green (+34), 33/33 e2e, typecheck + lint clean, **`pnpm eval:run` byte-identical** (precision 1, recall 0.9609375, Solis 22/22). Applied to hosted production: 0039 → 0040 → backfill → reclassify. **Round-3 seeds ranked in the baseline doc**, led by narrowing the digit rule (the violator list is its test set) and the five 100%-null valuation parsers.

**Previously:** **Data-quality / connections / observability pass — PRP phases 1, 2, 3 and 5 (2026-07-27).** **(1) `project_events` is idempotent.** Migration `0035_project_events_unique.sql` deletes the surplus and adds `project_events_dedupe_ux` on (project_id, source_record_id, event_type, event_date, observed_at) `NULLS NOT DISTINCT`; `emitEvent` now names it via `ON CONFLICT DO NOTHING`. **`observed_at` in the key is the whole design**: the obvious 4-column key collapses 2,997 rows but only 2,861 are duplicates, so it would have destroyed 136 real `applyRecordUpdates` re-emits. `NULLS NOT DISTINCT` matters because `event_date` is null on 383 rows. Local PG 16.4 and prod 17.6 both verified >= 15 before writing the DDL. **APPLIED TO PRODUCTION 2026-07-27 on owner instruction, and the prediction held exactly: 44,111 -> 41,250 (2,861 deleted), 0 residual duplicates on the 5-column key, and the 4-column key still reports 136 — i.e. every legitimate `applyRecordUpdates` re-emit survived and only same-instant repeats collapsed.** Prod was 35/35 migrations before, so 0035 was the only pending one and no unrelated DDL rode along. Null `event_date` rows went 383 -> 377, so 6 of the collapsed pairs were exactly the ones a unique index WITHOUT `NULLS NOT DISTINCT` would have let duplicate forever. A test reproduces the ACTUAL duplicate source — the non-transactional gap where the event commits and the resolution does not — because calling `resolveRecord` twice cannot: `record_resolutions_active_ux` rejects the second write before `emitEvent` is reached. **(2) The review queue drains itself on authoritative evidence.** `reevaluatePendingReviews` re-asks the resolver about every parked review and acts ONLY on `official_id`/`explicit_reference` — never parcel or fuzzy, no timeout, no lowered threshold, age is not an input. Dry-run by default; wired into `runMaintenance` with `apply: true` after resolve/update and before `scoreAll`. Merge when the id AGREES with the parked candidate, reject-and-re-resolve when it DISAGREES (the shape of all 46 split permits). Stamped `decided_by='system/reevaluation'`, so it is auditable and undoable. To kill preview/apply drift structurally, the pass ladder was EXTRACTED into one `decideResolution`; `resolveRecord` executes its verdict and `previewResolution` reports it. **Live dry-run: 2,000 scanned, 15 would resolve (8 official_id + 7 explicit_reference), 0 errors, 0 mismatched.** 15 is small — the 46 were already repaired by hand — and the value is ongoing prevention of pass-1b holds accumulating, not a big one-off drain. **(3) The 784 `same_address_name_mismatch` rows are NOT a name-matching problem — proposal killed by measurement.** `pnpm review name-audit` says **0 of 784 collapse under containment; 100% `none`**, and structurally so: the compared strings are permit titles ("20260190 - SFR - Remodel" vs "20260325 - Plumbing"), not company names. `classifyNameAgreement` compares an L&I registration to a Google listing; pointing it at permit titles is a category error no threshold can fix. They need org names or parcel evidence instead. **(4) Google Place staging.** Verified live: 7,460 rows, 3,882 `applied_at IS NULL`, 3,578 null `name_basis` (all applied). `--restage` rewrites payload ONLY — never `applied_at`/`applied_action` (the loader's provenance) and never `trust_score` (re-scoring an applied decision is a retroactive edit, so drift is COUNTED as `trustDrift` and reported instead). `--report` groups the batch by (name_basis, contested). This repo still cannot LAND them; the boundary is documented, not crossed. **(5) Resolver errors are durable.** `ResolveRunSummary.errors` is now `{sourceRecordId, error}[]` and raises a `resolver_errors` alert carrying every id, keyed on (day, digest of ids) so repeats dedupe but a different failure set never gets swallowed. First alert built from in-run state rather than a query, hence `runAlerts.extraCandidates`. Google Place inserts now retry via `withConnectionRetry` (the stated `@otn/db` dependency blocker did not exist). **BLOCKED: phase 5.3 (`pnpm lint`).** A `config-protection` hook forbids editing `eslint.config.mjs`, and I did not work around it. Also correcting the plan: the claim that all 97 errors are in `.worktrees/` is FALSE — it is **69 worktree + 28 real** `no-undef` errors on Node globals in tracked `scripts/*.mjs`, which ignoring `.worktrees/` would never have fixed. 1135/1135 tests green (+8), typecheck clean.
**Earlier:** **Fleet scheduling + solicitations as a second record class (2026-07-27).** Two PRPs burned down: `source-fleet-flow-verification` and `webs-omwbe-solicitation-ingestion` (Phases 0–2). **Nothing in production ran on a cadence** — `registerSchedules()` is correct but the `pgboss` schema never existed, so every run in `source_runs` was manual. `.github/workflows/source-fleet.yml` now schedules daily/weekly/monthly fetch + the nightly chain; the default-branch trap is RESOLVED not noted (`gh repo view` confirms the default branch IS `claude/tmux-install-320aiz`, so `schedule:` fires). Runs are sequential per cadence (`pnpm source:run:cadence`) rather than per-source cron. **Two of the three "broken" sources were measurement artefacts.** `bellevue_permits_arcgis` was never broken — its "zero records ever" came from two ORPHANED runs whose counters were never written; it held 4,018 records the whole time and a clean run confirms it green. The 11/4/7 flaky signature is ONE cause and by design: three capture-fed `on_demand` sources failing when no capture is staged. Only `spokane_permits_arcgis` is genuinely blocked — by USER AGENT, not misconfiguration (200 with a browser UA, self-redirect 302 with ours, no robots.txt); disabled with the measurement recorded rather than spoofing the UA to evade it. All nine never-ran sources dispositioned: **zero** are "should be running but isn't". `assertSourceFlow` + `pnpm flow:check` iterate CONFIG then left-join runs (a never-run source has no rows to find), count `parsed + duplicate + unchanged` as usable output, and report orphan-only windows as `unknown` rather than zero — backtested against the real 6 days it flags bellevue and spokane and does NOT flag `lacey_permit_reports`, where a single-run check would have flagged 13 and been wrong about 11. New `source_no_flow` alert covers the gap `source_stale` structurally cannot see. **Bids Phase 0–2 shipped:** `NormalizedSolicitationRecord` + `insights.solicitations` (migration 0036) + one persist branch, with the union at the adapter boundary so all 36 adapters compile untouched; OMWBE and WEBS adapters with fixtures and drift invariants, both `enabled: false` pending activation. **WEBS finding that would have shipped a silent bug:** a postback with a valid ViewState and no `ASP.NET_SessionId` returns 200 with a full grid that is PAGE ONE AGAIN — a cookie-less scraper looks healthy and collects page 1 six times. 600/600 tests green, typecheck and lint clean. **BLOCKED (human):** GitHub Actions secrets (`DATABASE_URL`, `OBJECT_STORAGE_*`). **DEFERRED with evidence:** Phase 3 (port `tacoma_solicitations`) — all 14 production `bidding_confirmed` projects come from it, so porting before a solicitation→project resolver exists would take that stage to zero.

**Older:** **Maintenance chain run; resolver errors cleared; an authoritative permit number awaiting review no longer loses to a weaker pass (2026-07-27).** **(1) The 15 resolver errors were transient, not data-shaped.** All 17 then-unresolved `source_records` were structurally valid on inspection, and a plain re-run cleared every one — `processed 17, merged 8, created 7, skipped 2, errors 0` — with no code change. The 2 `skipped` are the Lewis and Seattle `source_canary` rows: `NON_PROJECT_RECORD_TYPES` excludes them by design, so they report `skipped` on every run forever and are not a backlog. The exact error strings are NOT recoverable — `resolveUnresolved` logs them and increments a counter but persists nothing, and that run's stdout was tailed. **No residue:** each of the 15 has exactly one active resolution, and the two `project_events` they carry are the by-design `project_first_seen` + stage pair. Carry forward: `resolveRecord` is NOT transactional (`mergeIntoProject` then `persistResolution` as separate statements) and `emitEvent` is a bare insert into a table with no unique constraint, so a blanket retry would duplicate events. **(2) `maintenance:run` executed against production.** The point of it was `registry-link`, which does not run in `resolve:run`: `scanned 23, bound 23, conflicts 0, byMethod {contractor_number_exact: 23}`, cashing in the strong keys the PALS batch stamped. Also geocode 223/166 (1 error), corroboration 16,231 projects, registry export 667 project facts, 2,693 projects trade-tagged, alerts fired 10 / deduped 4 with `staleScoreSkipped: 0`. **The de-route sweep fired for real** — `candidates 4, archived 4, skippedManualState 0` — and `93054ce2` is now `archive` carrying `deroute {reason: no_longer_routed, previousState: weekly_digest, atAlgorithmVersion: 1.12.0}` with its 72.5 preserved beside it. **Every live opportunity is now 1.12.0** (1,660 priority + 2,583 digest); the single remaining 1.9.0 row is that archived one, which is the design. **(3) The PALS `new_project` rows exposed a RESOLVER defect, and the first fix was the wrong one.** The benign guess — "PALS holds permits the open-data layer does not publish" — is false: `pals-hydrate-export.ts` draws its pool exclusively from existing `pierce_permits_arcgis` records (spec §5 lookup-class hard rule), so every PALS id is one we already hold. Root cause: `matchByIds` can only see ids that reached `project_external_ids`, which is written only by `registerExternalIds` from `mergeIntoProject`/`createProject` — both need a project. **A record parked in `resolution_reviews` has no project, so the strongest key we have is invisible to the resolver until a human answers an unrelated question.** Exposure is **46, not 31**: 31 created a second project, and **15 more merged on `parcel_overlap` into a project that diverges from their twin's review candidate** — all 46 twins point elsewhere, so deciding those reviews splits one permit across two projects permanently. **I first shipped a pool filter that skipped parked permits (`125e3b7`) and then REVERTED it**: it stranded the licence data for exactly the population that needs it, since 30 of the 31 twins are parked at `fuzzy_without_parcel_or_org_support` — blocked for lack of the org evidence PALS carries — and it did nothing about the 15 parcel merges. Real fix: resolver **pass 1b** (`pendingReviewForSamePermit`), placed BEFORE parcel and fuzzy, parks the record next to its twin as `official_id` / `same_permit_pending_review`. Self-heals both ways — the twin gets a project, registers the number, and the held record merges on `official_id`. **`ResolveOptions.adjudicating` is load-bearing:** `decideReview` marks a row `rejected` before re-resolving, so without it the two records hold each other forever; a test asserts that deadlock. 1127/1127 green. **CLOSED 2026-07-28 — self-healed, verified 0 split permits graph-wide; now guarded by the `splitExternalIds` invariant in `pnpm audit:data`.** ~~OPEN, owner call: the 31 existing orphan projects and the 15 mis-merged parcel matches are left in place — real permit data, uncorroborated, each a latent split. Repointing or deleting production projects is not a unilateral call.~~

## Durability track (docs/roadmap-strengthening.md — Phase D)

| Task | Status | Evidence |
|---|---|---|
| D1 parser self-reconciliation invariants | ✅ 2026-07-16 | `source-sdk/invariants.ts` (reconcileCount/reconcileSum/checkNumericRange/checkDateWindow/checkPattern); optional `SourceAdapter.checkInvariants` called by the runner per artifact, violations stored in `source_runs.metrics_json` + counted; `evaluateSourceHealth` red on any. Lacey census reconciles the PDF's own printed permit count + unit total + valuation grand total (the manual-audit checks, now every fetch) + units-shape ceiling; Lewis inspections guards permit-id format + column-shift (permit/date bleeding into type/reason). Zero false positives on both live layouts; catches dropped-row, column-swap, column-shift. 15 tests (7 unit + 5 adapter + 3 health) |
| D2 field-fill (null-rate) instrumentation | ✅ 2026-07-16 | runner accumulates per-run fill over `MONITORED_FILL_FIELDS` (address/dates/valuation/units/geometry/orgs) → `source_runs.metrics_json.fieldFill`; `evaluateSourceHealth` red when a field ≥50%-present drops >20% relative vs the source's previous parsed run (spec §14, self-referential so structurally-null fields never trip). 3 health tests (drop→red, stable→green, sparse-both→green) |
| D3 schema-fingerprint drift action | ✅ 2026-07-16 | `evaluateSourceHealth` compares the two most recent parsed runs' `schema_fingerprint` (hash of raw field names, recorded since M0 but never compared); a change raises amber with a "review the parser" reason — a visible canary, not silent. 2 health tests (changed→amber, stable→green) |
| D4 substitute-coverage dependency | ✅ 2026-07-16 | config `mitigates: string[]` (wa_sepa → pierce_environmental_determinations, tumwater_development_review, tumwater_sepa); the M4.7 `source_red` alert escalates when a red source is a substitute — names the now-uncovered dependents in message + `details.mitigatedDependents`. Map built from config by `alerts:run`. 1 alerts test (enriched vs plain message) |
| D5 parser replay by parserVersion | ✅ 2026-07-16 | migration `0005_replay_meta` (`raw_artifacts.discovery_meta_json`); `replaySource` re-parses stored immutable bytes through the current adapter, rewrites only changed `source_records.normalized_json` (raw artifacts read-only; evidence append-only, backfilled only when absent); idempotent under an unchanged parser; audit `source_runs` row (`metrics_json.replay=true`); `pnpm source:replay <key>`. 3 tests (corruption restored from artifact, idempotent re-run, audit row) |
| D6 connection-drop durability (pooler) | ✅ 2026-07-27 | `aa986ad` + `57d55e6`; plan `.claude/PRPs/plans/pooler-durability-pass.plan.md`. **Prevent:** `createPool`/`createRegistryPool` carry `idleTimeoutMillis: 10_000` + `keepAlive` + `connectionTimeoutMillis` (node closes idle sockets before Supavisor's reaper, so a dead one is never handed back). **Recover:** `withConnectionRetry` in `@otn/db` matches `err.code` *and* message and walks `cause`/`AggregateError.errors[]` — the real 2026-07-27 failure was a DrizzleQueryError whose own message was the SQL, with `ETIMEDOUT` two levels down. Applied to `runner.ts` terminal write, `score-run.ts`, `geocode.ts`, `velocity.ts` (guard+insert as ONE unit), `registry-observations.ts` export (two SEPARATE retries — the pair spans two databases). **NOT** applied to `resolver.ts` `emitEvent`: `project_events` has no unique constraint and dedup is impossible because `applyRecordUpdates` legitimately re-emits the same (project, record, type) on a stage advance. **Detect:** `reapOrphanedRuns` closes runs stuck in `running` past 60 min as `completed_with_errors` / `orphaned_no_terminal_write`; runs first in `runMaintenance`, plus `pnpm runs:reap [--dry-run]`. Threshold MEASURED over all 80 production runs (slowest legitimate = pierce 8.8 min / 6,145 records). `evaluateSourceHealth` excludes orphans from `successes` (else `lastSuccessAt` is refreshed by a run that never succeeded), ambers on one / reds on two consecutive, and skips the count-based checks whose inputs an orphan never wrote. 25 new tests; 1101/1101; eval gates byte-identical. Bellevue's 2 stuck rows reaped live — it now reads **red** with the exact reason where it read green. |
| **Phase D exit** | ✅ 2026-07-16 | Durability track complete D1–D5. 230/230 vitest. Silent positional mis-parse now caught (D1), required-field collapse caught (D2), schema drift surfaced (D3), substitute-coverage concentration alerted (D4), parser fixes replayable over immutable history (D5). |

## Milestone ledger

| Task | Status | Evidence |
|---|---|---|
| M1.0 shared infra (checkpoints, HTTP helper, HTML extraction) | ✅ 2026-07-15 | runner checkpoint round-trip tests; `packages/documents` html.ts |
| M1.1 Lacey REST + project pages | ✅ 2026-07-15 | 79+79 records live, 0 rejected, health green, idempotent rerun + 90-day backfill verified; ledger in source-policy.md |
| M1.2 Lewis current planning + canary | ✅ 2026-07-15 | 14 applications live + canary (4 official links incl. discovered SmartGov host), green, idempotent rerun verified |
| M1.3 Lewis issued-permits PDF | ✅ 2026-07-15 | golden 06-28 PDF (wrapped rows, applicant/contractor split); 16 PDFs → 385 permits, 90-day backfill + checkpoint rerun green |
| M1.4 Pierce environmental determinations | ⛔ blocked 2026-07-15 | Cloudflare challenge (non-bypassable); ledger records blocker + M1.8 mitigation |
| M1.5 King public notices | ✅ 2026-07-15 | 170 notices (155 with parcels), unincorporated-King jurisdiction, idempotent rerun green |
| M1.6 King monthly reports (Excel) | ✅ 2026-07-15 | 8 reports → 1,446 issued + 474 applications, header-keyed dual-layout parser, checkpoint rerun + backfill green |
| M1.7 Seattle Socrata ×2 + canary | ✅ 2026-07-15 | 2,117 building + 118 land-use (applieddate high-water; nightly-republish caveat), canary green, 90-day backfill green |
| M1.8 WA SEPA register | ✅ 2026-07-15 | official data.wa.gov dataset mmcb-z6jf (separ UI robots-restricted); 564 pilot-county records, rerun + backfill green; Pierce mitigation live |
| M1.9 Thurston active notices | ✅ 2026-07-15 | 63 project notices (accordion parser, migration-canary throw), idempotent rerun green |
| M1.10 Tumwater ArcGIS + pages | ✅ 2026-07-15; capture-fed live-verified 2026-07-19/20 | ArcGIS: 44 projects, official status-domain stage map, green. review/SEPA pages: Akamai bot-fingerprints the CLIENT — REACHABLE in-region for a GENUINE BROWSER (curl/automated still 403); parser built + run live against operator-captured DRC agendas + SEPA notices, 0 invariant violations (59 SEPA leads), `cadence: on_demand` permanently (never datacenter-scheduled by design) |
| **M1 exit gate** | ✅ 2026-07-15 | 13 sources enabled all green, 5,555 records, 83/83 tests + E2E, backfills verified, blockers documented |
| M2.1 resolution normalizers | ✅ 2026-07-15 | address/parcel/org/name/stage/geometry + extractFeatures; 13 unit tests |
| M2.2 exact/lineage/parcel matching | ✅ 2026-07-15 | migration 0001_resolution; live corpus: 4,986 projects, 571 merges, 377 multi-record projects, 0 errors; cross-source merges verified (SEPA↔Seattle MUP, King notices↔reports, Thurston↔SEPA, Lewis↔SEPA, Lacey REST↔pages) |
| M2.3 fuzzy/geospatial + review gates | ✅ 2026-07-16 | passes 4–5 (address+name, proximity+org, PostGIS ST_DistanceSphere ≤75m); §10 gates: same-address TI, generic names, fuzzy-without-support → review; resolver 0.3.0 |
| M2.4 development/phase hierarchy | ✅ 2026-07-16 | name-base grouping w/ permit-vocab distinctiveness guard + org/parcel/proximity support; plat-parents phases; 26 developments / 60 projects live; derived layer rebuildable |
| M2.5 merge-review/split workflow | ✅ 2026-07-16 | decideReview merge/reject (rejected candidates excluded from re-resolution), undoResolution keeps records + history, review CLI |
| M2.6 permit-cluster velocity | ✅ 2026-07-16 | one cluster_velocity signal per development (≥5 permits/90d) on the anchor project; idempotent until a newer permit; live corpus max cluster = 4/90d → correctly silent |
| **M2 exit gate** | ✅ 2026-07-16 | 0 records lost (5,553 resolved + 2 canaries by design); 377 evidence-backed cross-source projects; 31 developments; 10,412 events; review/undo workflow proven; 121/121 tests + E2E |
| M3.1 account profiles + versioned rules | ✅ 2026-07-16 | 3 seeded pilot accounts from account-profiles.yaml; append-only rule versioning (edit = version max+1); 3 integration tests |
| M3.2 routing + deterministic scoring | ✅ 2026-07-16 | pure §12 scorer (components 0–1 × raw integer weights = 0–100), distinct per-account routing proven on live corpus (4,982 projects → 2,343 opportunities; At Home 247 King-excluded / Commercial 1,007 / Solis 1,089); sticky manual states; `pnpm score:run` |
| M3.3 model extraction pipeline + budgets | ✅ 2026-07-16 | migration `0002_model_runs`; §13 Zod contract (unknown evidence IDs reject the run); Anthropic provider (official SDK, `claude-opus-4-8`, key-activated) + MockProvider; per-job + monthly budget blocks *before* spend; every attempt persisted with tokens/cost/latency/result hash; visible blocked state without keys; 17 tests (7 contract + 10 integration) |
| M3.4 independent verifier + publication gate | ✅ 2026-07-16 | verifier: second model pass, one verdict per fact vs its cited evidence (fabricated/missing verdicts reject); gate: all 9 §15 checks deterministic over stored rows (red-source suppression, A-grade core event, D-never/C-not-alone grades, pending-review contradiction, 180d timing, threshold, verifier verdict); live corpus: 988 digest-band opportunities → 986 `blocked_on_verifier` (correct without keys) + 2 honest identity fails (SEPA records with unmappable stage); 16 tests |
| M3.5 §16 UI + §17 APIs | ✅ 2026-07-16 | all §16 routes + §17 endpoints 1:1; pilot HMAC-cookie auth, account isolation enforced in the query layer and proven by E2E (cross-account read → 404, customer → admin API → 403); opportunity page: facts-vs-inferences, evidence w/ grades+retrieval dates, timeline, gate checks, next action, feedback/state controls; admin: sources (run enqueues pg-boss job, disable), run detail w/ dead letters, review merge/reject, coverage; 9/9 Playwright E2E |
| M3.6 idempotent weekly digest | ✅ 2026-07-16 | §18: 5 sections, gate-passing items only, withheld items disclosed in section 5; per-item what-changed/why-it-fits/facts/inferences/next-action/source-links; "new" = first-ever inclusion (delivery history), unchanged repeats never labeled new; idempotent per (account, week) incl. never re-sending; Mailpit send verified; live run: 3 drafts, 767 withheld pending verification (honest keyless state); 4 integration tests |
| M3.7 feedback + disposition reasons | ✅ 2026-07-16 | controlled disposition vocabulary enforced at the API (free-form rejected 400, proven E2E); dismiss/feedback forms use the vocabulary; per-account calibration rollup (rates incl. honest nulls when unanswered, disposition counts, per-route relevance) on /app/feedback + `pnpm feedback:report`; never auto-applied to rules; 3 integration tests + E2E |
| M3.8 reviewed samples per account | ✅ 2026-07-16 | `docs/pilot-samples.md`: 7 per account from the live corpus, every fact matched verbatim to stored A-grade evidence + source URL; 3 misses found and logged as §22 calibration items (turf-field wrong_trade at 85, min-job-size, cross-source unit-count discrepancy stated not resolved) |
| **M3 exit gate** | ✅ 2026-07-16 | zero unsupported facts (0 records w/o evidence, 0 live model claims, 0 delivered items, 0 bidding_confirmed); distinct routing proven incl. same project → different routes per account (7096544-CN: Commercial joint_review vs Solis radar); feedback loop complete (capture → controlled dispositions → rollup → versioned rule edit); pilot runs without spreadsheets (UI + review queue + digest + CLIs); 172/172 vitest + 9/9 E2E. Independent-verifier *execution* pending keys — publication correctly held at `blocked_on_verifier` |
| M4.1 eval harness + labeled set + holdout | ✅ 2026-07-16 | `fixtures/eval/eval-set.v1.jsonl`: 200 examples (125 positives: 50 AH / 25 LGC / 50 SI + 75 hard negatives), 150 dev / 50 holdout stratified; features + labeling clock frozen per example → deterministic replay; reviewer rejections auditable in `review-exclusions.v1.json`; `pnpm eval:build` / `pnpm eval:run`; 4 harness unit tests; baseline measured (precision 97.3% ✅ / recall 72.3% ❌ → M4.2) |
| M4.2 precision/recall/duplicate/expiry gates | ✅ 2026-07-16 | scorer v1.1.0: residential-glass trade recency (glass installs months after permit — 180d window), lot-count text parse → repeatable-units/scale, unknown-stage timing 0.5 (neutral, not worst-case guess), mechanical-replacement → non-interior trade, field-work-only ⇒ never Division 08 (M3.8 fix). Eval: dev 100%/96.8%, holdout 100%/93.5% (gates ≥90%/≥80%), 0 hard-negative leaks; duplicate/expired measured over stored deliveries via per-item metadata (`pnpm delivery:metrics`, 0/0 live; metric proven to detect fabricated violations); live re-score: AH 12 priority/153 digest (was 0/67) |
| M4.3+M4.4 controlled automation policy | ✅ 2026-07-16 | `gate/automation.ts` (policy v1.0.0): auto-include requires gate pass + independent verification all-supported + every fact ≥0.9 confidence + no missing critical facts; high-risk categories (deadline/actionable claim, ≥$5M, ambiguous routing, contact data, bidding_confirmed) ALWAYS human — the only override is a recorded human decision (promoted); withheld items go to the digest reviewQueue (stored in delivery metadata with reasons, count disclosed in section 5, never silently dropped); 7 policy unit tests + 3 digest integration tests |
| M4.5 P1 sources from measured gaps | ✅/⛔ 2026-07-16 | gap measurement: Thurston permit layer missing (Lacey/county/Olympia 0 permit-stage). Activated: `lacey_permit_reports` (monthly census PDFs, 2 layout variants, parser totals reconcile with printed totals; 67 permits Jan–Jun incl. 24/24/30/89-unit MF; 2 held in review correctly) + `lewis_inspections` (daily PDF, FINAL→near_final §12.1 signal; 4/15 merged into existing permit projects via explicit reference). Blocked/deferred with evidence: `thurston_activity_reports` (WebLink robots `Disallow: /`) and `olympia_smartgov_reports` (session-stateful Exago viewer — needs approved headless adapter). Ledger updated; 9 adapter tests |
| M4.6 invitation ingestion scaffolding | ✅ 2026-07-16 | migration `0003_private_sources` (sources.account_profile_id + artifact_access_log); CustomerBidInboxAdapter reads customer-provided exports (never scraped/credentialed), the ONLY path to `bidding_confirmed` and only for explicit invitation statuses; private records excluded from the shared graph (resolver skip — documented scaffold boundary); /app/invitations + API scoped by owning account with §20 access audit; wrong-account exports refused; contact data never leaves rawFields; source ships disabled pending Solis authorization; 5 integration tests |
| M4.7 operational alerts | ✅ 2026-07-16 | migration `0004_alerts`; spend (80% warn / 100% critical), source-red (daily while red), stale >2× cadence, unsent-draft digests; idempotent per period (reruns dedupe, proven), email exactly-once via Mailpit (verified through Mailpit API); `pnpm alerts:run [--send]`; live run: 16 enabled sources evaluated, 0 fired (all green — honest); 3 integration tests |
| **M4 exit gate** | ✅ (measured) / ⏳ (key-gated) 2026-07-16 | **Measured now:** priority precision 100% dev / 100% holdout (gate ≥90%), recall 96.8%/93.5% (≥80%); duplicates 0 & expired 0 over stored deliveries with the metric proven to detect violations (<3%/<2% gates); zero unsupported facts (0 evidence-less records, 0 live model claims, 0 delivered items, 0 bidding_confirmed projects); 203/203 vitest + 9/9 E2E incl. §19 full-chain and account isolation. **Key-gated:** 30–60 min review/account/week is measurable only once model keys enable live delivery cycles — the structural pieces (verified-only auto-include, high-risk review queue, alerts) are in place; Solis priority volume (349) must first come down via §22 calibration |
| **Strengthening roadmap (addendum) — non-human-gated build** | | |
| Phase D sourcing durability (D1–D5) | ✅ 2026-07-16 | parser self-reconciliation invariants + golden-diff canary, §14 field-fill-drop red trigger, schema-fingerprint drift canary, substitute-coverage escalation, parser replay over immutable artifacts |
| Phase D follow-through (real instrumentation) | ✅ 2026-07-16 | **D2**: per-source `required_fields` in `config/sources.yaml` (lacey_permit_reports, lewis_inspections) → health adds an absolute-floor check (catches a source that *starts* broken) + scopes the relative-drop check. **D1**: `checkInvariants` extended beyond the 2 PDF parsers to the Seattle Socrata + King Excel adapters (value-range + date-window self-checks on JSON-API / spreadsheet column drift). +6 tests |
| M2.6 depth — campus velocity (#3) | ✅ 2026-07-16 | `computeCampusVelocity`: clusters DISTINCT-named, NOT development-grouped projects by 6-digit parcel-block prefix (excludes residential subdivisions, already a cluster_velocity concern); ≥5 with active permits in 90d → one `campus_velocity` signal on the anchor. Surfaces a campus (SpaceX SE02–SE06) as one big account instead of N disconnected opportunities. Live: 17 campuses found incl. SpaceX block 720232. `campus_velocity` added to the taxonomy (divergence-of-record, like cluster_velocity). +2 tests |
| Campus surfacing (#1) + per-county prefixes (#5) | ✅ 2026-07-17 | Migration `0013_campus_block`: derived `projects.campus_block` (`'<county>:<block>'`), stamped/cleared by `computeCampusVelocity` every run (clear-then-stamp, county-scoped when filtered) — scoring/UI/digest read one indexed column. `PARCEL_BLOCK_PREFIX_BY_COUNTY` calibrated on the live corpus (King 6 = official PIN major, max group 16; Lewis 6 = base parcel, max 4; Thurston 7 = plat+block, max 5; Pierce default 6 pending data — activity gate is the primary false-positive control). Scorer v1.4.0: `ProjectFeatures.campusBlock` (optional → frozen eval byte-identical, GATES PASS 100%/96%); commercial scale_value floor 0.6 + `active_campus` signal; Solis signal-only (provisional §12.3); At Home unaffected (campus ≠ residential cluster — proven by test). Opportunity page "Active campus" sibling panel (`campusSiblings`); digest `DigestItem.campus` + rendered context line. Live: 17 campuses, 94 stamped, SpaceX King:720232 10/10 opportunities signaled. +9 tests (4 scoring unit, 3 velocity, 2 digest) |
| Lead-time backtest (#2) | ✅ 2026-07-17 | `packages/delivery/src/leadtime.ts`: `evidenceLeadTime` (per account — days between the earliest dated event on a project's timeline and its permit-issued milestone; measured-only, nulls never fabricated zeros) + `detectionLagBySource` (first_seen_at − event_date; all-time and trailing-30d medians — backfill inflates all-time by design). Surfaced on /app/roi + `/api/app/roi`. **Live baseline (honest):** median advance notice 0d across all three accounts — most opportunities' first dated sighting IS the permit; ≥30d notice: AH 3.6%, LGC 2.8%, SI 1.3%, driven entirely by cross-source SEPA/entitlement→permit merges. Product implication recorded: lead time grows by widening entitlement-stage coverage, not by tuning scoring. Detection-lag note: every source was ingested within the last 30d (project began 2026-07-15), so recent == all-time until the corpus ages; king_permit_reports 76.9d median lag reflects monthly-in-arrears publication, lacey 397d reflects long-lived project pages. +4 tests |
| P1 — GC league table + targets | ✅ 2026-07-17 | `orgActivityRollup` (deterministic; relevance = the router's opportunity rows; placeholder/individual names flagged not deleted) + `relationshipTargets` (relevant + unworked; blocked/worked excluded); /app/organizations league table + "Worth meeting" panel; API view=league|targets; migration `0016` `organizations.registry_ref` (OTN-registry join point, import-only). Live Solis targets face-valid (dev partners/engineering firms, 3–4 routed projects each). +3 tests, +1 E2E |
| P2 — 10-minute digest + one-tap + outreach prep | ✅ 2026-07-17 | Easy-win cut (stage ≤60d + named org + valuation band + service radius; per-account `easy_win` config — Solis values PROVISIONAL until Sunday); top block: Winnable now (≤3) / GCs worth meeting (≤2) / Deadlines (inbox) / Radar (≤2); §18 sections unchanged below. One-tap actions hardened per security review (CRITICAL fixed pre-merge: GET renders confirm page only — mail-scanner prefetch safe; POST consumes the single-use hashed token and applies audited effects; rate limit, no-store/no-referrer, DB CHECKs, migrations `0017`/`0019`). Memos carry evidence-only talking points (P2.4). +6 tests, +2 E2E |
| P3 — timing intelligence | ✅ 2026-07-17 | `stage_lag_stats` (migration `0018`) recomputed nightly from same-record applied→issued pairs; memos append "typically issues in ~N weeks" (county × class, n≥20, labeled historical inference); /app/radar early-stage pipeline page + digest radar section. +2 tests, +1 E2E |
| P4 — mobile pass | ✅ 2026-07-17 | Real viewport; tables scroll within themselves on phones; facts/inferences grid stacks; phone-viewport E2E (390×844) asserts no page-level horizontal overflow on list/pursuits/map |
| P5 — tacoma_solicitations activated + Solis inbox enabled | ✅ 2026-07-17 | First public procurement source (ledger entry): Tacoma's open-solicitation HTML tables (robots allows all) — the spec's "explicit solicitation" class, the ONLY public signal permitted to set `bidding_confirmed`; controlled automation still holds every such item for human review. 3 pages → 17 solicitations, 0 errors; idempotent unchanged-hash rerun; 17 projects resolved. `customer_bid_inbox_solis` enabled + account-bound (authorization 2026-07-17). +3 tests. Eval GATES PASS (100%/96%); 334/334 vitest; 21/21 E2E (1 transient flake re-run green) |
| Suburb monthly-PDF sweep + Olympia access test | ✅ 2026-07-18 | **Centralia: monthly permit reports FOUND** (cityofcentralia.com DocumentCenter, Jan 2025→Feb 2026, per-permit rows incl. contractor + est. cost; Feb-2026 PDF fetched 200 from our egress) — ready to build `centralia_permit_reports`. **Tumwater notices/SEPA page re-verified live** (items through 06/2026 incl. two preliminary plats) — unblocks the disabled config stubs; adapter build required. Lakewood = quarterly only (verify row-level before building); Bonney Lake monthly series discontinued post-CMS-migration (2021–22 archive only); Sumner none; Chehalis has a Permits Issued page but 403s all fetchers. **Olympia: block confirmed NETWORK-level** (stock Chromium → ERR_CONNECTION_RESET both hosts; datacenter-IP block, not client) — honest paths recorded (operator-local run or manual file drop); never bypassed |
| puyallup_permits_arcgis ACTIVATED (suburb-city SFR track) | ✅ 2026-07-18 | Largest Pierce suburb — Solis home-market SFR gap (PALS=unincorporated, Accela=Tacoma). Full 9-step checklist (ledger): publisher verified (ArcGIS org 5K6vnOH0GkPyJs6A = "City of Puyallup"; layer backs the official Permit Viewer app), 25,595 rows since 2008 incl. Residential New SFD ×1,484 + Pre-Application ×343/120d. Data quirks handled honestly: IssueDate is a STRING column with mixed epoch/MM-DD-YYYY values (normalize both, unparseable→null; window on typed dates only, 210d); FeeAmount=fees never valuation. Live: 2,126 parsed/0 rejected, idempotent unchanged-hash rerun, "Administrative Approval" status found live→mapped→D5 replay (952 corrected), backfill Sep–Dec 2025 +364. Resolution 1,557 → 506 merged/933 created/116 review/0 errors; Solis +132. Sweep notes recorded: Lakewood/Bonney Lake/Sumner/Centralia/Chehalis have no hub dataset (next: monthly-PDF check per city); Olympia 403 blocker stands. +4 parser tests |
| Olympia both reports (issued + applications) reconcile exactly; home-metro operator-local capture proven; Solis calibration decisions | ✅ 2026-07-19/20 | Olympia P1 gap fully closed: `olympia_smartgov_reports` now parses BOTH rolling 30-day SmartGov reports — issued (572) and, new this pass, the applications/submitted report (815) — each self-reconciling exactly against its own printed category + Grand Total (`checkInvariants` empty, `parserVersion 1.1.0`); `discover()` un-gated to emit both (Insights commit `5cb924f`). The applications report is the pre-issuance signal `docs/domain-bid-timing.md` calls for: commercial interiors are bid 2–6 months *before* the permit issues, so issued-only data structurally missed that window. Three positional-parse defects fixed to get there: a project-description line starting with the word "TOTAL" was mis-read as a category total (swallowed 11 records, misread a permit-# fragment as a 4-digit count); wrapped permit-number suffixes ("0399"/"0482") were mis-detected as bogus category headers; an over-strict length gate missed a wrapped total's count when it shared a line with a label fragment. Same pass: Tumwater DRC + SEPA captured live via the operator's real Chrome and parsed with 0 invariant violations (59 SEPA leads) — proves `pnpm source:run:operator-local` end to end. Docs: `docs/runbooks/registry-seam-golive.md` restructured (Part A operator-local capture, new; Part B the registry seam, preserved); new `docs/solis-requirements.md` requirements contract; `config/sources.yaml` corrected. **Solis calibration decisions (owner, 2026-07-20, not yet implemented):** home-metro geo up-weight vs King commercial (Pierce ~equal); `permit_applied` weighted ABOVE `permit_issued`; small `warm_gc_active` weight; layered easy-win bands ≤20/≤35/≤50 mi, no floor. Still open: priority threshold, `verified_gc_on_project` weight, easy-win age window. 9 adapter tests green |
| v1.8.0 — Solis routes residential new construction | ✅ 2026-07-17 | Customer-approved: every new home needs drywall+paint. `routeSolis` fits gains SFR new-build in HOME counties (Pierce/Lewis/Thurston; King deferred to calibration); demolition/field-work/entitlement-only never ride in. Clustered/subdivision new-builds → ONE `gc_relationship_radar` play (trade_fit 0.45, `production_builder_pipeline` signal — §10 forty-permit rule); custom builds direct (0.7, `new_home_construction`). Live: Solis 2,139→3,151 opps (+1,012), priority 469→669, 236 builder relationship plays; weekly ≥80 flow 13→25. Eval v2 GATES PASS 1.00/1.00 with ZERO flips — **the frozen set has no home-county new-SFR examples** (predates Pierce/Tacoma); class recorded as eval-uncovered → add examples at next eval refresh. +4 tests |
| Paint bid-window + PNW season caveat | ✅ 2026-07-17 | Second Solis trade on its own clock (customer domain model, docs/domain-bid-timing.md): residential paint bids 6–12 wk post-issuance (open through wk 14; finish trade — execution ~12–16+ wk), commercial paint rides the same pre-permit GMP buyout (+ late-mobilization caveat once issued, 4–12+ mo execution). `tradeBidWindow`/`tradeBidWindows` generalize the module; digest items carry 🔨+🎨 lines; easy-win needs ≥1 non-closed trade window; residential paint whose execution midpoint (issued+14 wk) lands Nov–Apr gets the deterministic wet-season heads-up (>50°F/dry-surface constraint — tent-and-heat or interior-first). No scoring change needed (residential timing is stage-level; both windows union under issued/construction which already peak). +7 unit/integration tests |
| Commercial pre-permit sourcing track | ✅ 2026-07-17 | Verified live: **no new source needed — every county already has a pre-permit feed** (table in docs/domain-bid-timing.md). The gap was interpretation: Pierce PALS + Tacoma Accela pre-application cases walked the permit stage map, so an "Approved"/"Final" *screening* became an approved/complete *project* (411 overstated in Pierce alone). Parser **1.1.0** both adapters (pre-app cases pin to `preapplication`; cancelled→withdrawn kept); D5 replay 13,864+6,202 re-parsed → 436+180 corrected, 0 errors; 424 project stages recomputed (max over active records — documented one-off, laterStage never regresses on its own); rescore + radar now shows preapplication-band Solis items. v1.7.0 timing now sees honest stages. Remaining candidates (Olympia/Lacey pre-app logs, Seattle DRB, hearing examiner) recorded as verify-first breadth, not holes. +5 adapter tests |
| Bid-window → prioritization (scorer **v1.7.0**) | ✅ 2026-07-17 | Customer directive: items whose 🔨 line says "likely passed" must not rank priority. `timingInterior` is now bid-track-aware: commercial-track issued/construction → 0.5/0.4 (buyout done; addendum re-pricing keeps it non-zero), in-review commercial stages peak at 1.0; residential map unchanged. `commercial_bid_window_likely_closed` signal added for auditability; easy-win cut refuses `likely_closed` windows (a "Winnable now" item can't carry a "Likely passed" line). `bidding_confirmed` explicitly exempt (confirmed-open fact). Eval v2: Solis precision 1.00 / recall 1.00 HELD, GATES PASS. Live rescore: Solis priority 482→469. Lacey routes untouched (no validated glazing timing model) |
| A — digest humanization (deterministic) | ✅ 2026-07-17 | Owner-readable rendering, model keeps raw audit values: stage/place phrases, verified facts as (path,value) pairs humanized at render ("$150,000 job value", "permit issued Jun 3, 2026"), missing-fact keys → plain words ("who the GC is", "whether it's out to bid"), route/signals telemetry → one "Why it fits" sentence (phrase maps, unknown keys degrade to de-snaked words), "Next step" no longer leaks fact keys. §18 headings/disclosure strings unchanged. Phone-render verified |
| B — drywall bid-window signal | ✅ 2026-07-17 | `drywallBidWindow`+`bidTrackFor` (packages/intelligence, from docs/domain-bid-timing.md customer domain model): residential = 4–8wk AFTER issuance (open through wk10, opens_soon/likely_closed math off stated issueDate; null date → watch, never fabricated); commercial = pre-permit CD buyout (in-review stages → OPEN now; issued → likely_closed w/ addendum caveat); `bidding_confirmed` fact beats the model both tracks; every note "typically" (inference). Surfaced as the 🔨 line on interior-trades digest items only (trade-specific); digest query gains latest_issue_date. 14 unit + 1 integration tests. Live: Seattle TI correctly classifies commercial-track → "likely already let, addendum possible" |
| Pre-Sunday #1 — calibration prep package | ✅ 2026-07-17 | `docs/calibration-prep-solis.md` + rerunnable `apps/worker/calibration-sensitivity.mts`: threshold table (65–95 standing + changed-7d/30d — 482 standing vs ~13 changed/week at 80), easy-win radius×age grid + valuation bands (60→80 km adds 0; $100k floor 65→46; $2M cap excludes nothing), Solis priority mix (King 334 / Pierce 148; issued+applied dominant), At Home totals (1,053 priority; 856 unincorporated Pierce + 191 Tacoma = 99.5%) with A/B/C decision options. sigmap-verified |
| Pre-Sunday #2 — digest rehearsal (live) | ✅ 2026-07-17 | Fresh Solis build week-ending 07-19: top block renders (2 GC plays), spec §18 sections + gate disclosure honest (480 blocked-on-verifier, 20 gate-failed → **model keys needed before first real Monday send**); phone-viewport screenshot verified. Found+fixed: 8 synthetic E2E invitations in the REAL Solis inbox (unique Message-ID per run → now stable `e2e-inv-stable@`, residue purged) and Deadlines display-dedupe on (title, gc, due, scope) — re-forwarded unmatched invitations legitimately duplicate rows |
| Pre-Sunday #3 — org name hygiene | ✅ 2026-07-17 | `splitOrgNameAddress` + `orgNameKey` (packages/resolution, conservative: split only at house-number+street-token or PO BOX with ≥1 leading name token); league rollup groups suffix variants (`variantNames`, summed counts, county union), flags recomputed on the split name + `address_in_name`; graph orgs NEVER merged (display only). Live: fused-address individual gone from Solis targets; Smith Fire/Patriot Fire variants each one row. +6 unit +2 integration tests |
| Pre-Sunday #4 — action-token retention | ✅ 2026-07-17 | `cleanupActionTokens` (30d past used_at/expires_at; live tokens untouched; effects = durable audit) wired into nightly maintenance; ops runbook updated. +1 test |
| Pre-Sunday #5 — source health sweep | ✅ 2026-07-17 | **All 19 real sources GREEN.** Root causes fixed, not suppressed: lacey_permit_reports had 0 runs — D2 health tests were resetting the *real* source's history (new disabled `fake_source_required` fixture declares required_fields; tests repointed); Seattle/SEPA fingerprint ambers = benign sparse-JSON oscillation (Socrata omits null keys) — D3 now ambers only on a fingerprint unseen across the recent-run window (new-schema still trips; +1 test); fixture-class sources excluded from customer coverage caveats. lacey_permit_reports re-run (4 PDFs → 50 records) + resolve (127 processed, 59 merged, 53 created, 13 review, 16 stage advances, 0 errors) |
| Batch3 #1 — review-queue triage | ✅ 2026-07-17 | `triageReviewQueue` clusters pending reviews by (rule, primary reason, candidate project) with counts/score-range/sample titles; `decideReviewCluster` bulk-decides a cluster through the per-row `decideReview` path (identical provenance, per-row error collection, rerun-safe). Live queue shape: 1,425/1,483 were one pattern (proximity_org fuzzy-without-support) over ~315 candidates. UI: clusters table + bulk buttons on /app/admin/review (reject = safe mass action; merge candidate-gated + confirm); CLI `pnpm review triage` / `review bulk`; API cluster/decision endpoint. Queue intentionally left for the human. +3 tests, +1 E2E |
| Batch3 #2 — opportunity list filters | ✅ 2026-07-17 | `listOpportunities` → filter object (state incl. "all"/archive, county, stage, q over name/jurisdiction/address, campusOnly, sort score/recent, limit/offset) returning `{items,total}` via window count; page gets filter form, campus badge, count line, pagination; API passes params + total through. +1 E2E (API filters + page) |
| Batch3 #3 — tacoma_permits_arcgis activated | ✅ 2026-07-17 | Full M1 checklist (ledger in source-policy.md): Tacoma Open Data ArcGIS FeatureServer (Accela extract, daily pull_date; old Socrata portal is dead — verified). 109,728 rows; 120d-window adapter (1000/page, objectid order, day-quantized URLs); 52-value Accela status vocabulary → pattern-bucket stage map (unknown degrades, never guesses); applicant_name → confirmed applicant role; 0-valuation/units → null. Shadow 4,206 / 0 rejected / 0 errors; idempotent rerun 5 pages unchanged-hash; backfill Jan–Mar +1,617. Resolution: 5,825 → 1,954 merged, 3,669 created, 200 review, 0 errors; campuses 58 → 134 (1,087 stamped). Commercial priority 13 → 99 (Tacoma commercial core). Eval GATES PASS. 3 parser tests |
| Batch2 #1a — stage-change follow-through | ✅ 2026-07-17 | Root cause of flat lead time found: sources that republish a record in place (Seattle Socrata, Lacey REST, now Pierce) update `normalized_json` + fingerprint but `resolveUnresolved` skips actively-resolved records — **application→issuance transitions were silently dropped** (no stage change, no digest item). Migration `0015`: `record_resolutions.processed_fingerprint`; `applyRecordUpdates` (resolver.ts) re-processes drifted records — advances stage via `laterStage` (never regresses from one record), emits the stage-change event (source-stated event date), refreshes roles/geometry, marks version processed. Runs in `resolve:run` + nightly chain. First sweep: 5,568 checked, 0 advanced (honest — content was applied at resolution; the gap is forward-looking, now closed). +4 tests |
| Batch2 #1b — pierce_permits_arcgis activated | ✅ 2026-07-17 | Full M1 checklist (ledger in source-policy.md): county's official open-data FeatureServer (PALS extract) — NOT behind the Cloudflare that blocks M1.4. 679,941 rows; trailing-120d window adapter (day-quantized URLs, bounded 2000/page, deterministic order, backfill by date range). Shadow 6,001 records 0 rejected/errors; idempotent rerun; backfill Feb–Mar +1,452. Resolution: 7,455 processed → **2,131 merged cross-source** (SEPA↔permits), 3,839 created, 1,483 → review queue, 0 errors. Pierce projects 153 → 3,992 (97% with geometry). Campuses 17 → 58. Pierce parcel prefix calibrated: 6 (10-digit parcels, max structural group 21). 5 parser tests. Eval GATES PASS (100%/96%) |
| Batch2 #2 — stall protection + alert egress | ✅ 2026-07-17 | `catchUpMaintenance`: on boot, if no maintenance run completed in 24h and none queued → enqueue immediately (singleton-keyed; chain idempotent). Alert EMAILS: nightly chain passes `send: true` to runAlerts when `ALERTS_EMAIL` set (M4.7 exactly-once semantics). +2 tests |
| Batch2 #3 — geocode backlog drained | ✅ 2026-07-17 | Final drain: 1,376 attempted → 1,131 matched (82%), 112 no-match, 106 ambiguous, 25 county-mismatch, 2 transient (auto-retry). Corpus location coverage incl. Pierce influx: 7,789/8,897 projects (88%) |
| **§22 calibration item: At Home Pierce priority volume** | ⏳ customer | With real Pierce data, At Home priority went 12 → 862: genuine §12.1 matches (plats, townhomes, multifamily — e.g. "Sunset Pointe 18-lot plat", "JD Villas 24 apartments") in a county that was always in At Home's configured territory but had zero coverage until now. avg score ~80.6, i.e. borderline over the 80 threshold. NOT retuned unilaterally (same posture as the Solis-349 finding): customer should choose Pierce sub-territory, a higher priority threshold, or a capacity snapshot. Deliveries remain verifier-gated — no customer impact meanwhile |
| Self-driving cadence (#4) | ✅ 2026-07-17 | `apps/worker/src/schedules.ts` — `registerSchedules` reconciles pg-boss cron from `config/sources.yaml` at every worker boot: 15 enabled sources scheduled per cadence (daily 02–03:xx PT with deterministic per-source minute stagger; weekly Mon; monthly on the 2nd; on_demand/private-class never scheduled; stale schedules dropped when a source is disabled/removed); `pipeline-maintenance` nightly 04:45 PT (resolve → developments → cluster/campus velocity → geometry materialize → geocode 500 → score → M4.7 alerts — the exact manual CLI chain, same functions); `digest-draft` Mon 05:15 PT builds idempotent DRAFTS only (sending stays human). Verified by live worker boot (17 schedules logged) + 5 tests (cadence mapping/stagger, schedulable filtering, idempotent reconcile, stale-drop). |
| Geometry fill + Census geocoder + map view (#3) | ✅ 2026-07-17 | Found during gap measurement: **project geometry was 0 rows** — the resolver's `fillGeometry` post-dates the corpus and nothing backfilled it, while 2,345 records carry geometry. Migration `0014_geometry_provenance` (`geometry_source`, `geocode_meta_json`); `packages/resolution/src/geocode.ts`: `materializeProjectGeometry` (record geometry → projects, 'source_record', also runs in `resolve:run`) + `geocodeProjects` (US Census geocoder — official/free/keyless, verified live 2026-07-17, proxy-aware; street-only addresses guarded by unique-match + returned-county cross-check; every attempt outcome persisted so reruns skip; transient errors retried). `pnpm geocode:run`. Live: 2,224 King materialized from records; 660 geocode attempts → 463 matched (70%), 0 errors → **2,801/5,058 projects located (55%)**; backlog ~1,600 King addresses (nightly chain, #4). UI: `/app/map` (Leaflet circleMarkers by band, geocoded points labeled as inferences in popups, OSM attribution) + geometry-coverage-by-provenance panel on admin coverage. Ledger entry for the geocoder appended to source-policy.md (enrichment-only, never a record source). +5 tests, +3 E2E (15/15) |
| Account-isolation fix: private-class sources vs shared graph | ✅ 2026-07-17 | Found via a live `resolve:run` during #1: the shared-graph guard keyed ONLY on `sources.account_profile_id IS NULL`, so an UNBOUND private inbox source (e.g. after the bid-inbox test teardown, or any not-yet-activated inbox) leaked its records into the shared graph — 2 fixture invitations became shared projects. Fix: `sharedGraphSourceGuard()` (resolver.ts) additionally excludes `access_class = 'private_authorized'` on any binding state; leaked fixture projects removed from the dev corpus; regression test (unbind → resolve → 0 resolutions). |
| S0 capacity snapshots | ✅ 2026-07-16 | migration 0006; versioned `[from,to)` snapshots; deterministic explained `assessCapacity`; score-run folds capacity factor into `rationale_json.capacity`; provisional Solis seed; 10 tests |
| S1 decision memo | ✅ 2026-07-16 | migration 0007; typed versioned `OpportunityDecisionMemo` (content-hash idempotent regenerate); deterministic assembly over gate/extraction/capacity/score; procurement state never calls a permit a bid; API + opportunity-page panel; 4 tests + E2E |
| S2 pursuit pipeline | ✅ 2026-07-16 | migrations 0008/0009; server-side state machine (§4 transitions, human-only estimating/submitted/won/lost/no_bid, reasons/dates required, audited); Kanban + detail UI; API; 8 tests + E2E |
| S3 invitation ingestion | ✅ 2026-07-16 | migration 0010; deterministic `.eml` parser; idempotent ingest; ambiguous→review; deadline-change events preserve both deadlines (verified=false); .eml upload + webhook API; 6 tests + E2E |
| S4 GC relationships | ✅ 2026-07-16 | migration 0011; account-specific relationship state distinct from public roles; blocked/do_not_pursue suppress alerts; contact provenance; GC view UI; 3 tests + E2E |
| S5 ROI + trust controls | ✅ 2026-07-16 | migration 0012; scorecard reproduces §8 metrics from stored events; human-flagged attribution; suppression-before-assembly; append-only corrections; urgent-alert whitelist; scorecard UI; 4 tests + E2E |
| S6 Lacey sample | ✅ 2026-07-16 | `docs/lacey-sample.md` — 5 reviewed live opportunities (3 At Home + 2 Commercial) + 1 honest calibration miss (SpaceX demo over-scores division_08); shared pipeline, zero bespoke Lacey code (guarded by cost-control tests) |
| **Roadmap exit posture** | ✅ (built) / ⏳ (gated) 2026-07-16 | 265/265 vitest + 13/13 E2E; every phase's exit-gate logic proven. Gated remainder: S0 real calibration (customer), memo/extraction prose + review-time gate (keys), live customer authorizations. New §22 item: commercial trade-fit demolition/entitlement negative filter (SpaceX finding) |
| M0.1 workspace/apps/packages/lint/type/test | ✅ 2026-07-15 | `pnpm lint` / `pnpm typecheck` clean |
| M0.2 Docker Compose + .env.example | ✅ 2026-07-15 | `pnpm infra:up` — Postgres/PostGIS, MinIO, Mailpit |
| M0.3 schema/migrations/seed | ✅ 2026-07-15 | migration `0000_init` (20 tables), idempotent seed (18 sources, 3 accounts) |
| M0.4 object store + immutable artifacts | ✅ 2026-07-15 | content-addressed MinIO store, sha256 verified round-trip |
| M0.5 pg-boss jobs/retries/dead-letter/logs | ✅ 2026-07-15 | `apps/worker/test/jobs.test.ts` |
| M0.6 manifest loader + fixture harness | ✅ 2026-07-15 | `fake_source` adapter + fixtures |
| **M0 exit gate** | ✅ | `apps/worker/test/m0-exit-gate.test.ts` — 24/24 tests, E2E 1/1 |
| De-route sweep + delivery version guard | ✅ 2026-07-26 | **Code is in `fb200c5`, whose subject says `docs(plan): reconcile counts to three sightings` — see the provenance note below.** `apps/worker/test/score-run-deroute.test.ts` — 9/9; full suite 1076/1076, 108 files; lint + typecheck clean; `eval:run` GATES PASS byte-identical |
| CRM upgrade: takeoff scaffold + field communication (deck A5/A6) | ✅ 2026-07-28 | migration `0038_takeoff_field` (4 tables); `packages/intelligence/src/takeoff.ts` (deterministic evidence→assembly derivation, provenance on every derived line, re-derive NEVER touches manual edits) + `field.ts` (multi-use hashed crew links — deliberately not action_tokens, whose single-use invariant one-tap email depends on; daily logs; change orders decided exactly once, audit note on decision); `/field/{token}` crew page (no session, plain HTML forms, api/action hardening) + cockpit takeoff worksheet & field section; CO email via idempotent `field_notify` deliveries row (env-gated SMTP, send failure never fails the crew POST). 21 unit + 4 e2e tests; e2e 33/33; typecheck clean. Unit costs + valuation shares are OWNER-ASSUMED placeholder figures — calibrate with Solis before trusting a stamped estimate |

## Commit provenance (read before trusting `git log --oneline` here)

Two sessions were writing this repo concurrently on 2026-07-26, and one committed with a catch-all `add`. The result is a commit whose subject describes one change and whose contents are mostly another. History was deliberately NOT rewritten (the commit was already pushed to the shared trunk, and force-pushing under an active concurrent writer risked destroying its work) — so the mapping is recorded here instead.

| Commit | Subject says | Also actually contains |
|---|---|---|
| `fb200c5` | `docs(plan): reconcile counts to three sightings` | **The entire de-route sweep + delivery algorithm-version guard** — 12 files, 771 insertions: `scoring.ts` (`scoredByCurrentAlgorithm`, `hasRouter`), `score-run.ts` (the sweep, `DEROUTE_REASON`, `deroutedOpportunities`, `opts.projectIds`), `digest.ts`/`render.ts`/`alerts.ts` (the guard + its disclosure), `score-run-deroute.test.ts` (new, 380 lines), three fixture files, and these docs. Only the 14-line `pooler-durability-pass.plan.md` hunk matches the subject. |

Rule of thumb for anyone (human or agent) walking this history: `fb200c5` is a **merged** commit in the informal sense — `git show fb200c5 --stat` is the honest description, not its subject line. `aa986ad` (durability pass) later moved `withConnectionRetry` into `@otn/db` and migrated the sweep's two call sites; the de-route tests were re-run green after it.

## Open blockers

- **M1.4 pierce_environmental_determinations:** piercecountywa.gov serves a site-wide Cloudflare browser challenge (403) to this execution environment regardless of user agent; solving it would mean bypassing an anti-bot control — prohibited. Source stays disabled; blocker + mitigation (Pierce SEPA coverage via M1.8 wa_sepa) recorded in the activation ledger. Re-verify from an unchallenged network (production runner / customer connection).
- **M1.10 tumwater_development_review + tumwater_sepa — RESOLVED, operator-local by design (2026-07-19/20):** genuine-browser-only, same as Olympia. No longer a gap: both captured live via the operator's real Chrome and parsed with 0 invariant violations (DRC agendas + SEPA notices, 59 SEPA leads). `cadence: on_demand` in `config/sources.yaml` is a permanent architecture choice, not a temporary block — Akamai fingerprints the *client*, not just egress IP, so these can never be safely auto-scheduled in the datacenter; they run only via `pnpm source:run:operator-local` over staged genuine-browser captures (procedure: `docs/runbooks/registry-seam-golive.md` Part A). Mitigations (tumwater_development_arcgis, wa_sepa) remain as the datacenter-scheduled floor.
- **Olympia (`olympia_smartgov_reports`) — RESOLVED (2026-07-20):** same capture-fed pattern; both rolling 30-day reports (issued 572, applications 815) now parse and self-reconcile EXACTLY against their own printed totals (`parserVersion 1.1.0`, `discover()` emits both). The applications report is the P1 pre-permit gap fill — commercial interiors bid 2–6 months *before* issuance (`docs/domain-bid-timing.md`), so this is the earliest legitimate home-metro signal available. Operator-local by design, same rationale as Tumwater above.

## Assumptions in force

- Solis score-component weights are **provisional** placeholders (spec §12.3 forbids finalizing before calibration).
- Live sources are enabled one at a time as their activation checklists pass (ledger in `docs/source-policy.md`); Lacey REST + pages enabled 2026-07-15.
- Health volume-drop rule skips runs with `unchangedCount > 0` (hash-identical content is not a drop); required-field null-rate drop (spec §14) still needs per-run field instrumentation — tracked for M1 exit.
- Lacey REST exposes only the *current* project listing — "≥90-day backfill" for Lacey means the full current listing is ingested and the window filter is verified, not that delisted historical projects are recoverable.
- The runner's unchanged-artifact skip keys on content hash only: a parser upgrade does not reprocess already-stored artifacts until their content changes. Stored-artifact reprocessing (replay by parserVersion) is a future capability — not required by M1.
- Fuzzy passes 4–5 apply to records resolved after M2.3 (resolver 0.3.0); the M2.2-era corpus was resolved with passes 1–3 only. Retroactive project-level duplicate candidates surface through the M2.5 merge-review workflow, not by re-running the resolver.
- **Divergence of record:** `cluster_velocity` extends the spec §9 event list — §21 M2.6 requires velocity events but §9 doesn't enumerate a type for them. Documented in taxonomy.ts, data-dictionary, architecture.
- M4.6 scaffold boundary: private bid-invitation records deliberately do NOT merge into the shared project graph (a private invitation would move a shared project's stage, leaking one account's signal to others). Per-account graph overlays + scoring integration are activation-time work, designed once a real customer export shape exists.
- M2.6 backfill: type-specific events (permit_issued/application_submitted/…) were backfilled for the M2.2-era corpus (4,859 events) — createProject originally emitted only project_first_seen; it now also emits the creating record's own event.

## Pending calibration (spec §22 — gather from customers, unblocks M3 rule finalization)

- **Lacey:** King County coverage for At Home; builder appetite; minimum lots/units; apartment/townhome routing; product lead times; builder relationships.
- **Solis:** license renewal (registration researched through 2026-08-11 — recheck); detailed drywall/painting scope; capacity; min/ideal job size + geography; preferred/blocked GCs; public-work constraints; invitation platforms + authorized ingestion.
- **From S6 Lacey sample** (`docs/lacey-sample.md`): commercial `division_08` demolition/entitlement negative filter — **shipped, scorer v1.2.0** (`isDemolition && !hasBuildingScope` and bare-entitlement → radar). Demoted 182 demolition/field + 243 entitlement commercial projects; eval unchanged (dev 100%/96.8%, holdout 100%/93.5% — no regression). **Residual addressed structurally (scorer v1.3.0):** scope classification now runs **per source record** (aggregated by max), so a glazing keyword in a demolition/fire record can't borrow a sibling record's construction scope; tracing SpaceX SE04 revealed the "window" lived in an HVAC/ductwork record ("no change to exterior"), so a high-precision `noEnvelope` negative was added — SE04 `division_08_system_fit` 1→0.7 (no longer claims glazing). Eval byte-identical (blob path unchanged; gates hold). **Remaining boundary (§13, key-gated):** SE04 sits ~90 on *scale alone* (legit large-commercial radar, not a glazing claim); "does a bare CUP genuinely carry glazing scope" is a keyword-semantics call left to the model extraction/verification layer — more regex would risk demoting real large-commercial glazing work.
- **From M3.8 sample review** (`docs/pilot-samples.md`): ~~division_08 trade_fit needs a negative filter for site/field work (turf field scored 85)~~ **resolved** — M4.2 field-work demotion; Lakeside turf now scores 72.5 (digest), regression-guarded in `scoring.test.ts`. ~~briefs must present both citations when sources disagree on unit count (41st Ave: 198 vs 180)~~ **resolved** — deterministic `unitCountDisagreement` in the digest surfaces every stated unit count with its own citation; rendered as a "Sources disagree" caveat; tested in `digest.test.ts`. **Remaining (customer-gated):** minimum-job-size floors would demote low-valuation SFR permits and STFI residential remodels — still needs Solis's min/ideal job size before a threshold can be set (not a guess).

## Connections needed later

- M3: `ANTHROPIC_API_KEY` (and/or OpenAI) + `LLM_MONTHLY_BUDGET_USD`.
- M4/pilot ops: customer-authorized WEBS access, bid-inbox ingestion method, production SMTP.

## Tooling

- 2026-07-15: sigmap (code-signature grounding), sqz (command-output compression), and ast-grep (structural code search + outline) installed and made mandatory workflow tools — see CLAUDE.md "AI tooling (mandatory)" and `docs/operations.md` "AI tooling: sigmap, sqz, and ast-grep". No product-code impact.
- 2026-07-15: ECC (Everything Claude Code) curated install — 17 skills + 11 agents, **no hooks / no capture** (confirmed with user; ECC's global blocking/capture hooks deliberately excluded, plugin/`install.sh` path avoided). See CLAUDE.md "ECC skills and agents" and `docs/operations.md`. Project-scoped (`.agents/skills/`, `.claude/agents/`, `skills-lock.json`); every file read/scanned before use. No product-code impact; lint clean.
- 2026-07-15: Self-learning loop (custom, project-scoped) — after finding ECC's auto-learn hook is inert without per-call capture and its injector isn't separable, built a minimal Stop-extract + SessionStart-inject loop over `.claude/learned/LEARNED.md` (tracked). No per-call capture, fail-open, loop-guarded. `.claude/hooks/*`, `.claude/settings.json`; see `docs/operations.md` "Self-learning loop". Takes effect next session. No product-code impact.
